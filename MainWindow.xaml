<Window x:Class="LeHub.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:LeHub.ViewModels"
        xmlns:converters="clr-namespace:LeHub.Converters"
        mc:Ignorable="d"
        Title="LeHub"
        Height="700"
        Width="1100"
        MinHeight="500"
        MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundDarkBrush}"
        KeyDown="Window_KeyDown"
        AllowDrop="True"
        Drop="Window_Drop"
        DragOver="Window_DragOver"
        WindowStyle="None"
        ResizeMode="CanResize"
        x:Name="MainWin"
        StateChanged="Window_StateChanged">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0"
                      ResizeBorderThickness="6"
                      GlassFrameThickness="0"
                      CornerRadius="0"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:StringNotEmptyToVisibilityConverter x:Key="StringNotEmptyToVisibility"/>
        <converters:BoolToStarConverter x:Key="BoolToStar"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Border Grid.Row="0"
                Background="{StaticResource BackgroundMediumBrush}"
                BorderBrush="{StaticResource BorderSubtleBrush}"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- App Title (non-draggable) -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="LeHub"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontWeight="SemiBold"
                               FontSize="12"/>
                </StackPanel>

                <!-- Draggable empty zone (only this zone allows drag) -->
                <Border Grid.Column="1"
                        Background="Transparent"
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"/>

                <!-- Window Buttons (non-draggable) -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="MinimizeButton"
                            Style="{StaticResource WindowControlButton}"
                            Click="MinimizeButton_Click"
                            ToolTip="Reduire">
                        <Path Data="M0,5 L10,5"
                              Stroke="{StaticResource TextSecondaryBrush}"
                              StrokeThickness="1"
                              Width="10" Height="10"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Center"/>
                    </Button>
                    <Button x:Name="MaximizeButton"
                            Style="{StaticResource WindowControlButton}"
                            Click="MaximizeButton_Click"
                            ToolTip="Agrandir">
                        <Path x:Name="MaximizeIcon"
                              Data="M0,0 L10,0 L10,10 L0,10 Z"
                              Stroke="{StaticResource TextSecondaryBrush}"
                              StrokeThickness="1"
                              Width="10" Height="10"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Center"/>
                    </Button>
                    <Button x:Name="CloseButton"
                            Style="{StaticResource WindowCloseButton}"
                            Click="CloseButton_Click"
                            ToolTip="Fermer">
                        <Path Data="M0,0 L10,10 M10,0 L0,10"
                              Stroke="{StaticResource TextSecondaryBrush}"
                              StrokeThickness="1"
                              Width="10" Height="10"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Center"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0"
                Background="{StaticResource BackgroundMediumBrush}"
                BorderBrush="{StaticResource BorderSubtleBrush}"
                BorderThickness="0,0,1,0">
            <DockPanel Margin="12">
                <!-- Logo/Title -->
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,24">
                    <TextBlock Text="LeHub"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="Application Launcher"
                               Style="{StaticResource SubtitleText}"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Sidebar Actions -->
                <StackPanel DockPanel.Dock="Top">
                    <Button Content="+ Ajouter une appli"
                            Style="{StaticResource SidebarButton}"
                            Command="{Binding AddAppCommand}"/>
                    <Button Content="Gerer les tags"
                            Style="{StaticResource SidebarButton}"
                            Command="{Binding ManageTagsCommand}"/>

                    <Separator Background="{StaticResource BorderSubtleBrush}" Margin="0,12"/>

                    <!-- Presets Section -->
                    <TextBlock Text="PRESETS"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               FontSize="10"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>
                    <Button Content="+ Nouveau preset"
                            Style="{StaticResource SidebarButton}"
                            Command="{Binding AddPresetCommand}"/>

                    <ItemsControl ItemsSource="{Binding Presets}" Margin="0,8,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="Transparent"
                                        CornerRadius="4"
                                        Margin="0,2"
                                        Padding="8,6">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Name}"
                                                       Foreground="{StaticResource TextPrimaryBrush}"
                                                       FontSize="12"/>
                                            <TextBlock Text="{Binding AppsDisplay}"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       FontSize="10"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                        <Button Grid.Column="1"
                                                Content="▶"
                                                FontSize="10"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Foreground="{StaticResource AccentGreenBrush}"
                                                Cursor="Hand"
                                                Padding="6,2"
                                                ToolTip="Lancer le preset"
                                                Command="{Binding DataContext.LaunchPresetCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"/>
                                        <Button Grid.Column="2"
                                                Content="x"
                                                FontSize="12"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Foreground="{StaticResource AccentRedBrush}"
                                                Cursor="Hand"
                                                Padding="6,2"
                                                ToolTip="Supprimer"
                                                Command="{Binding DataContext.DeletePresetCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <!-- Version -->
                <StackPanel DockPanel.Dock="Bottom" VerticalAlignment="Bottom">
                    <TextBlock Text="Glissez un .exe ou .lnk"
                               Style="{StaticResource SubtitleText}"
                               FontSize="10"
                               TextAlignment="Center"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="v2.3"
                               Style="{StaticResource SubtitleText}"
                               HorizontalAlignment="Center"/>
                </StackPanel>
                <StackPanel/>
            </DockPanel>
        </Border>

        <!-- Main Content -->
        <DockPanel Grid.Column="1">
            <!-- Top Bar -->
            <Border DockPanel.Dock="Top"
                    Background="{StaticResource BackgroundMediumBrush}"
                    BorderBrush="{StaticResource BorderSubtleBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="16,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Box (LEFT) -->
                    <Grid Grid.Column="0" Width="300">
                        <TextBox x:Name="SearchBox"
                                 Style="{StaticResource SearchTextBox}"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Width="300"/>
                        <TextBlock Text="Rechercher... (Ctrl+F)"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center"
                                   Margin="12,0,0,0"
                                   IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding Text, ElementName=SearchBox}" Value=""/>
                                                <Condition Binding="{Binding IsFocused, ElementName=SearchBox}" Value="False"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <!-- Filters (RIGHT) -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <!-- Tags Filter -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                            <TextBlock Text="Tags:"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>
                            <ComboBox x:Name="TagsCombo"
                                      ItemsSource="{Binding TagFilterOptions}"
                                      SelectedItem="{Binding SelectedTagFilter}"
                                      DisplayMemberPath="Label"
                                      MinWidth="140"
                                      MaxWidth="200"/>
                        </StackPanel>

                        <!-- Favorites Toggle -->
                        <ToggleButton Style="{StaticResource FavoriteToggle}"
                                      IsChecked="{Binding ShowFavoritesOnly}"
                                      ToolTip="Afficher uniquement les favoris">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding ShowFavoritesOnly, Converter={StaticResource BoolToStar}}"
                                           FontSize="16"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text=" Favoris"
                                           VerticalAlignment="Center"
                                           Foreground="{StaticResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </ToggleButton>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Apps Grid -->
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="8">
                <ItemsControl x:Name="AppsItemsControl" ItemsSource="{Binding FilteredApps}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="CardBorder"
                                    Background="{StaticResource CardBackgroundBrush}"
                                    BorderBrush="{StaticResource BorderSubtleBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Cursor="Hand"
                                    MouseLeftButtonDown="Card_MouseLeftButtonDown"
                                    MouseRightButtonUp="Card_MouseRightButtonUp"
                                    Tag="{Binding}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Width" Value="240"/>
                                        <Setter Property="Padding" Value="12"/>
                                        <Setter Property="Margin" Value="6"/>
                                        <Style.Triggers>
                                            <!-- Responsive: Larger cards when maximized -->
                                            <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource AncestorType=Window}}" Value="Maximized">
                                                <Setter Property="Width" Value="300"/>
                                                <Setter Property="Padding" Value="16"/>
                                                <Setter Property="Margin" Value="8"/>
                                            </DataTrigger>
                                            <!-- Hover state - visible with accent border -->
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{StaticResource CardHoverBrush}"/>
                                                <Setter Property="BorderBrush" Value="{StaticResource AccentBlueBrush}"/>
                                            </Trigger>
                                            <!-- Selected state with shadow (higher priority than hover) -->
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter Property="BorderBrush" Value="{StaticResource AccentBlueBrush}"/>
                                                <Setter Property="BorderThickness" Value="2"/>
                                                <Setter Property="Background" Value="#404858"/>
                                                <Setter Property="Effect" Value="{StaticResource CardSelectedShadow}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Favorite Star (top-right, absolute position) -->
                                    <Button Grid.Row="0"
                                            Content="{Binding IsFavorite, Converter={StaticResource BoolToStar}}"
                                            FontSize="18"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Foreground="{StaticResource AccentYellowBrush}"
                                            Cursor="Hand"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Top"
                                            Margin="0,-4,-4,0"
                                            Padding="4"
                                            Command="{Binding DataContext.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            ToolTip="Ajouter/Retirer des favoris"/>

                                    <!-- Header: Icon + Name + Missing Badge -->
                                    <Grid Grid.Row="0" Margin="0,0,30,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Icon -->
                                        <Border Grid.Column="0"
                                                CornerRadius="6"
                                                Background="{StaticResource BackgroundMediumBrush}"
                                                Margin="0,0,10,0">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Width" Value="40"/>
                                                    <Setter Property="Height" Value="40"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource AncestorType=Window}}" Value="Maximized">
                                                            <Setter Property="Width" Value="48"/>
                                                            <Setter Property="Height" Value="48"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Image Source="{Binding Icon}"
                                                   Stretch="Uniform"
                                                   RenderOptions.BitmapScalingMode="HighQuality">
                                                <Image.Style>
                                                    <Style TargetType="Image">
                                                        <Setter Property="Width" Value="32"/>
                                                        <Setter Property="Height" Value="32"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource AncestorType=Window}}" Value="Maximized">
                                                                <Setter Property="Width" Value="40"/>
                                                                <Setter Property="Height" Value="40"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                            </Image>
                                        </Border>

                                        <!-- Name + Missing badge -->
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}"
                                                       Foreground="{StaticResource TextPrimaryBrush}"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <Border Background="{StaticResource AccentRedBrush}"
                                                    CornerRadius="3"
                                                    Padding="4,1"
                                                    HorizontalAlignment="Left"
                                                    Margin="0,2,0,0"
                                                    Visibility="{Binding IsExeMissing, Converter={StaticResource BoolToVisibility}}">
                                                <TextBlock Text="Introuvable"
                                                           Foreground="White"
                                                           FontSize="9"
                                                           FontWeight="SemiBold"/>
                                            </Border>
                                        </StackPanel>
                                    </Grid>

                                    <!-- Tags (limited to 2 + "+N") -->
                                    <ItemsControl Grid.Row="1"
                                                  ItemsSource="{Binding DisplayTags}"
                                                  Margin="0,0,0,8"
                                                  Visibility="{Binding TagsDisplay, Converter={StaticResource StringNotEmptyToVisibility}}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Style="{StaticResource TagChip}">
                                                    <TextBlock Text="{Binding}"
                                                               Foreground="{StaticResource TextSecondaryBrush}"
                                                               FontSize="10"/>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <!-- Spacer -->
                                    <Border Grid.Row="2"/>

                                    <!-- Action Buttons: Lancer | Supprimer | ... -->
                                    <DockPanel Grid.Row="3">
                                        <!-- Right side: More options button -->
                                        <Button DockPanel.Dock="Right"
                                                Style="{StaticResource SmallButton}"
                                                Width="32"
                                                Padding="6,4"
                                                ToolTip="Plus d'options"
                                                Click="MenuButton_Click"
                                                Tag="{Binding}">
                                            <Path Data="M2,5 A1.5,1.5 0 1,1 2,5.01 M7,5 A1.5,1.5 0 1,1 7,5.01 M12,5 A1.5,1.5 0 1,1 12,5.01"
                                                  Fill="{StaticResource TextSecondaryBrush}"
                                                  Width="14" Height="10"
                                                  Stretch="Uniform"/>
                                        </Button>

                                        <!-- Left side: Lancer + Supprimer buttons -->
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Lancer"
                                                    Style="{StaticResource PrimaryButton}"
                                                    Padding="12,5"
                                                    FontSize="11"
                                                    IsEnabled="{Binding CanLaunch}"
                                                    Command="{Binding DataContext.LaunchAppCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"/>

                                            <Button Content="Supprimer"
                                                    Style="{StaticResource DangerButton}"
                                                    Padding="10,5"
                                                    FontSize="11"
                                                    Margin="8,0,0,0"
                                                    Command="{Binding DataContext.DeleteAppCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"/>
                                        </StackPanel>
                                    </DockPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
        </Grid>
    </Grid>
</Window>
